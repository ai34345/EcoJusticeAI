# Virtual environments
venv/
.venv/
env/

# Secrets — never commit
.env
.env.local
*.pem
*.key
credentials.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
dist/
build/

# ML / third-party blobs (large, not source)
violations/ml/deep_sort/
violations/ml/model_data/

# Media & personal/demo artifacts (may contain identifiable faces)
output/
*.mp4
*.avi
*.mov
*.mkv
A1.png
result.jpg

# Keep intentional sample videos only if you re-add them under a consented path;
# local test copies at repo root stay ignored via *.mp4 above.
# Tracked test_videos/ already in history — remove from git if publishing.

# Notebooks / IDE
.ipynb_checkpoints/
.idea/
.vscode/
*.swp

# OS
Thumbs.db
Desktop.ini
.DS_Store
